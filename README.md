
- You may need to specify python >3.11
- bacpipe will automatically install and setup pre-trained models
- You will need linux (e.g. WSL) to run perch_v2

**BirdSet (huggingface) dataloader - bacpipe and datasets dependencies don't play nice, you're welcome to have another go at it**